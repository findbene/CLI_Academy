# content/

This directory will hold the learning content system.

Planned structure:

- `paths/` for lesson content
- `assets/` for downloads and supporting files
- `videos/` for snippet metadata later
- `schemas/` for lesson metadata contracts later

Every lesson should eventually support:

- freshness metadata
- platform metadata
- warning metadata
- quiz metadata
- lab metadata
- download asset references
