# PROGRESS.md

## Current phase

Repository normalization and control-plane hardening.

## Completed

- Defined target top-level scaffold
- Defined root operating documents
- Defined global and project-level `CLAUDE.md` contracts

## In progress

- Map existing folders to target structure
- Migrate root-level experiments and archives
- Separate active product code from historical scaffolding

## Next

1. Move `app/` to `apps/web/`
2. Move `backend/` to `apps/api/`
3. Move scripts into `tooling/scripts/`
4. Consolidate `.agentic`, `.agents`, `.antigravity` into `.claude/`
5. Archive `_communication/` and `_design_backup/`
