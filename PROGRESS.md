# PROGRESS.md

## Current phase

Zero-To-Mastery (ZTM) Infrastructure & Pedagogy Implementation.

## Completed

- **The ZTM Strategic Pivot:** Overhauled marketing pages and value propositions to highlight *Agentic Alumni Networks*, *Gamified Milestones*, and *Case Studies* (like NVIDIA NemoClaw).
- **Backend Architecture Re-write:**
  - Scaled up the rate limiter (`rate_limit.py`) using Redis caching and sliding windows to prevent horizontal bottlenecks.
  - Injected `python-json-logger` to format API telemetry automatically for Datadog/ELK parsing.
  - Enhanced the AI Tutor Agent with a dynamic "NemoClaw-Style" Orchestrator that intercepts system prompts and adapts its personality based on user conversation history (e.g., "Patient Mentor" vs "Infra Engineer").
  - Built the `gamification.py` backend endpoint to validate user JWT constraints before awarding daily streaks.
- **Curriculum Authoring (Phase 1):**
  - Fully authored the MDX payloads for `nano_claw_organizer.mdx` (Free Tier) and `nemoclaw_simulation.mdx` (Pro Capstone).
  - Drafted the `ZeroClaw Hello World` MDX lesson to complete the 3 Free Entry points block.
  - Adopted the Hybrid Model: Integrated code snippets directly in the MDX text *alongside* fully executable Python/JS starter/solution files packed in `public/assets/labs/`.
- **Lounge & Gamification Sub-systems:**
  - Prototyped the premium "Agent Lounge" UI with mock data using Framer Motion liquid glassmorphism.
  - Formally connected the Authentication Database endpoints for Gamification (`alumni_status`, `user_progress`, `achievements`).
  - Rapidly prototyped the secure "Alumni Command Center" (UI Dashboard).

- **ZTM Stabilization & Bug Fixes:**
  - Hardened the `uvicorn` and FastAPI deployment by ensuring `supabase` and `python-json-logger` dependencies are fully mapped and running in the background.
  - Cleared corrupted Next.js build cache (`.next/`) to resolve `[slug]` router collisions in the `app/(marketing)/` layout.
  - Successfully validated End-to-End frontend/backend synchronization and Agent Lounge dynamic MDX rendering via comprehensive Browser Subagent testing.
  - Restored the high-conversion "One-Liner" marketing punchline to the `HomePage` hero section UI.
- **Audit Remediation Pass (2026-04-09):**
  - Restored the Lounge as a public surface by moving the published-content implementation onto `/lounge` and deleting the stale tracked backup route artifact.
  - Replaced broken Lounge placeholder references with real local SVG assets and added rendering support for custom `Screenshot` and `TerminalSimulation` content blocks.
  - Tightened backend startup/config handling by loading `.env` before router import, requiring admin Supabase credentials for gamification paths, and upgrading readiness checks to return `503` when critical dependencies are missing.
  - Hardened auth accessibility and smoke coverage by wiring explicit input labels, adding accessible provider button names, and updating Playwright to assert against actual runtime behavior instead of test-runner env guesses.
  - Verified the remediation with clean frontend `lint`, clean production `build`, green Playwright smoke suite (`16/16`), and backend `compileall` success.

## In progress

- Editorial QA on the rewritten live curriculum, especially command realism and learner outcomes in the newer Paths 11-19 rewrites.
- Regression hardening for learner-flow surfaces is now in steady-state verification mode: the dashboard hydration, fallback/backfill, and tutor slices all execute locally through either QA credentials or the saved Playwright learner storage state in `apps/web/e2e/.auth/learner.json`.
- Follow-through work now that strict guided-step enforcement is live across the validator and CI gates.

## Next

1. Continue editorial QA on the rewritten live paths, prioritizing Paths 11, 12, 14, 15, 16, 18, and 19.
2. Harden the gamification service further around concurrency and malformed database edge cases.
3. Reconcile path-overview completion state with browser-local fallback so `/learn/[pathSlug]` does not lag behind the lesson page/dashboard after local-only saves.
4. Refresh the saved learner Playwright auth state whenever the QA session changes so the authenticated tutor and learner-flow suites stay runnable locally.

## Latest update

- Implemented the next academy follow-through pass across all five approved areas after the 2026 polish landed: seeded real flagship-course depth directly inside the live lesson corpus by attaching asset-vault packs and richer descriptions to key Claude Code, Cowork, Runtime Lab, and Capstone lessons; tightened `/learn/[pathSlug]` so learner path pages now carry academy framing, studio focus, milestone badges, and chapter-standard expectations; launched first-class `/runtime-lab`, `/workflow-studio`, `/prompt-doctor`, and rewritten `/labs` shells; expanded discoverability through nav/footer/sitemap/downloads links; and deepened the Asset Vault positioning around install-ready and course-linked packs. Revalidated with clean frontend `npm run lint`, clean `npm run typecheck`, and clean production `npm run build` in `apps/web`.
- Reran the authenticated Playwright tutor and learner-flow pack during this pass. It is still blocked by environment auth, not product behavior: the run now fails consistently because `apps/web/e2e/auth.ts` reaches the interactive-login fallback and no `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` are configured, while the saved storage state alone is not enough for this environment. Treat that as the active release-signoff blocker until fresh credentials or refreshed storage state are supplied.
- Implemented the five approved 2026 academy upgrades across the live shell instead of leaving them as planning-only notes. The shared academy model now carries explicit 2026 freshness micro-modules, chapter media standards, the new Course 21 mastery track for cost engineering and multi-provider agents, badge metadata, and optional showcase highlights. The public academy surfaces now render those upgrades on Home, Learn, Setup Academy, Prompt & Context Studio, Asset Vault, and the signed-in dashboard. The Asset Vault also gained four new seed downloads: `chapter-mini-notes-series`, `2026-runtime-and-deployment-update-brief`, `cost-engineering-multi-provider-playbook`, and `capstone-showcase-submission-kit`.
- Continued the academy-shell follow-through after publishing the initial install-to-production reframe on `main`: clarified route roles so `/learn` is the primary academy front door, `/paths` is the detailed live execution catalog, `/asset-vault` / `/downloads` are the builder-asset surfaces, and `/resources` is the broader ecosystem directory; updated the affected marketing/app copy and cross-links accordingly; and revalidated with another clean frontend `npm run lint`, clean `npm run typecheck`, and clean production `npm run build` in `apps/web`. Also reran the authenticated Playwright tutor and learner-flow pack outside the sandbox; the suite still fails before any product assertions because no usable authenticated learner credentials are configured in the current environment, so that remains an environment/auth-state blocker rather than a new academy-shell regression.
- Reframed the academy shell around the new product promise without disturbing the live lesson runtime: added shared academy metadata for fast-path weeks, spine milestones, and setup tracks; expanded the public IA with `/learn`, `/setup-academy`, `/prompt-context-studio`, and `/asset-vault`; refreshed marketing/layout copy to foreground the install-to-production moat; extended learner-facing surfaces with spine-project progress context on the dashboard and path pages; upgraded the asset taxonomy around field manuals, prompt packs, playbooks, and starter packs; and added new Week 1 / Setup Academy builder assets plus a durable planning document at `docs/curriculum/cli-academy-2026-install-to-production-plan.md`. Revalidated with clean frontend `npm run lint`, clean `npm run typecheck`, generated Next route types via `npx next typegen`, and a clean production `npm run build` in `apps/web`.
- Release-prep uncovered and fixed one real runtime regression after the broader validation loop was already green: loading `/learn/01-start-here/lesson-1-2-1-make-a-personal-safety-checklist` in the live dev server threw `useTutorRuntime must be used inside TutorRuntimeProvider` from `LessonStepCard` during initial server rendering. The root layout provider was intact; the issue was that this Next.js App Router path still rendered the client component tree on the server before the lesson runtime bridge was hydrated. Added `useOptionalTutorRuntime()` in `apps/web/components/tutor/TutorRuntimeProvider.tsx`, switched `apps/web/components/lesson/LessonStepCard.tsx` to the optional hook, and guarded the help-launch path plus learning-mode reads so lesson steps can prerender safely without weakening the strict hook for true provider misuse elsewhere.
- Revalidated that SSR/runtime fix end to end with the reliable app-local Playwright binary path: the corrected smoke rerun passed at `20/20`, the previously failing lesson route rendered cleanly in the browser with step cards and tutor launcher visible, and a full post-fix validation loop passed again (`npm run lint`, `npm run typecheck`, strict `python tooling/scripts/validate_content.py`, and `npm run build`).
- Release-prep validation on 2026-04-10 confirmed one more operational nuance: in this VS Code terminal environment, repo-root Playwright calls can still lose `--config` when routed through `npm`, so the reliable explicit-config fallback is the app-local Playwright binary under `apps/web/node_modules/.bin`. Using that path, the smoke pack passed cleanly at `20/20`; the direct authenticated rerun then failed before product assertions because no usable learner auth was available in the current environment (saved storage state was insufficient and `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` were unset). Treat that result as an environment/auth refresh blocker, not as a learner-flow or tutor regression.
- Release-prep verification confirmed the root-level untracked `test-results/` tree was disposable output from a mis-scoped Playwright smoke invocation, not a product regression. Added `test-results/` to the repo `.gitignore`, documented that smoke coverage must run from `apps/web` or with `apps/web/playwright.config.ts`, and kept the app-local Playwright config as the authoritative smoke-test entry point because `e2e/smoke.spec.ts` intentionally uses relative routes against its configured `baseURL`.
- Removed the last local blocker to authenticated learner-flow and tutor regression execution. Added `apps/web/e2e/auth.ts` so `apps/web/e2e/learner-flow.spec.ts` and `apps/web/e2e/tutor.spec.ts` now accept either `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` or a saved Playwright storage state, added `apps/web/e2e/.auth/` to the frontend `.gitignore`, saved a real signed-in learner state to `apps/web/e2e/.auth/learner.json`, and revalidated the full authenticated regression pack with `npx playwright test e2e/tutor.spec.ts e2e/learner-flow.spec.ts` (`9/9` passing) plus a clean production `npm run build` in `apps/web`.
- Tightened the signed-in lesson tutor mobile layout after live browser verification exposed a second overlap regression on `/learn/...`: when the tutor dialog opened, the fixed launcher stayed mounted underneath it and the open panel still reserved space for that old launcher, leaving the bottom app nav visible behind the modal. Updated `apps/web/components/tutor/FloatingTutor.tsx` so the launcher disappears while the dialog is open and the mobile dialog now uses `bottom-2` instead of the old launcher-reserved inset, added an authenticated regression assertion in `apps/web/e2e/tutor.spec.ts` that the launcher count drops to zero once the dialog opens, and revalidated with clean editor diagnostics, clean frontend `npm run lint`, plus live signed-in lesson QA showing `Open AI tutor` count `0` and `Close AI tutor` count `1` while the dialog is open.
- Finished the remaining visual browser pass on `/` and `/lounge` and found one real mobile regression instead of a runtime failure: the floating tutor launcher was globally mounted from `app/layout.tsx` and obscured marketing hero content on small screens. Scoped `apps/web/components/tutor/FloatingTutor.tsx` to learning/app routes only (`/dashboard`, `/downloads`, `/labs`, `/learn`, `/onboarding`, `/settings`), added a smoke regression in `apps/web/e2e/smoke.spec.ts` asserting `/` and `/lounge` do not render the launcher, and revalidated with clean editor diagnostics, clean frontend `npm run lint`, live browser checks confirming the launcher is absent on `/` and `/lounge` but still present on `/dashboard`, and a green `npx playwright test e2e/smoke.spec.ts` run from `apps/web` (`20/20` passing).
- Manually revalidated the new dashboard hydration reconciliation in a live signed-in browser session on `/dashboard`: with browser-local progress reset to empty, the dashboard showed `3` completed lessons and pointed continue-learning at `lesson-1-2-2`; after seeding a synthetic local completion, the completed-lessons card advanced to `4`; after resetting and marking the current continue-learning lesson complete only in browser-local storage, the dashboard advanced to `lesson-1-3-1-complete-the-first-tiny-project` and showed `4 of 7 lessons completed`; local storage was then restored to empty.
- Added the remaining learner-flow dashboard hydration regressions in `apps/web/e2e/learner-flow.spec.ts`: one seeds a synthetic browser-local completion and verifies the dashboard completed-lessons card increases during hydration, and the other marks the current continue-learning lesson complete in browser-local storage and verifies the dashboard advances to a different next lesson after reload. Revalidated with clean in-editor diagnostics, clean frontend `lint`, clean `typecheck`, targeted `playwright test e2e/learner-flow.spec.ts` execution (`4` skipped because `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` were not configured locally), and a clean production `build`.
- Expanded the authenticated Playwright learner-flow regression suite with two new slices in `apps/web/e2e/learner-flow.spec.ts`: one verifies browser-local completion fallback when hosted `POST /api/progress` returns `404`, and the other seeds anonymous local progress, verifies authenticated backfill posts `completionData.source = local-backfill`, and confirms the local entry is removed after a successful hosted save. At the same time, `apps/web/e2e/tutor.spec.ts` gained coverage for lesson code-block rendering integrity and free-tier gating of Pro tutor modes. Revalidated with clean in-editor diagnostics, clean frontend `lint`, clean `typecheck`, targeted Playwright execution (`7` skipped because `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` were not configured), and a clean production `build`.
- Added two authenticated Playwright lesson regressions in `apps/web/e2e/tutor.spec.ts`: one verifies the visible scaffold changes across Guided, Hint-based, and Independent learning modes on a live Path 01 step card, and the other verifies step-local tutor launch with mocked `/api/tutor` responses plus mode-aware request routing (`independent` -> `explain`). Revalidated with clean frontend `lint`, clean `typecheck`, targeted Playwright execution (`3` skipped because `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` were not configured), and a clean production `build`.
- Promoted the guided-step validator into enforcement mode: live walkthrough lessons now fail missing `stepSchemaVersion` or `StepMeta` coverage by default, `--strict-step-schema` remains as the explicit CI alias, and `--allow-missing-step-schema` is the opt-in compatibility mode for out-of-band audits.
- Updated `.github/workflows/ci.yml` so the content job now runs the explicit strict guided-step gate instead of the earlier migration-era validator invocation.
- Refreshed the public Agent Lounge surface to match the shared CLI Academy brand system: `/lounge` now clips decorative overflow to prevent sideways scrolling, the landing hero/cards/filters use the app's paper-and-teal palette instead of a separate dark theme, and the article detail view now follows the same lounge styling. Revalidated the lounge pass with clean `npm run lint` and clean `npm run build` in `apps/web`.
- Migrated all nine Path 19 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full capstones, portfolio proof, and job-ready evidence path now uses authored guided scaffolding instead of inferred fallback support.
- Migrated all seven Path 18 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full real-world agent builds path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 18 and Path 19 rollouts and confirmed the migration finished cleanly at `139` lessons across `19` paths with `0` errors and `0` warnings, completing the explicit guided-step schema rollout across every live path.
- Migrated all eight Path 17 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full secure integrations path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 17 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `16` non-blocking warnings, so the remaining schema backlog now starts at Path 18 instead of Path 17.
- Migrated all eight Path 16 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full computer-use, sandboxes, and browser automation path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 16 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `24` non-blocking warnings, so the remaining schema backlog now starts at Path 17 instead of Path 16.
- Migrated all eight Path 15 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full secure remote setups path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 15 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `32` non-blocking warnings, so the remaining schema backlog now starts at Path 16 instead of Path 15.
- Migrated all seven Path 14 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full secure local machines and safe defaults path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 14 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `40` non-blocking warnings, so the remaining schema backlog now starts at Path 15 instead of Path 14.
- Migrated all seven Path 13 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full multi-agent patterns path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 13 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `47` non-blocking warnings, so the remaining schema backlog now starts at Path 14 instead of Path 13.
- Migrated all seven Path 12 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full skills, memory, heartbeats, and scheduled-work path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 12 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `54` non-blocking warnings, so the remaining schema backlog now starts at Path 13 instead of Path 12.
- Migrated all seven Path 11 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full OpenClaw and runtime foundations path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 11 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `61` non-blocking warnings, so the remaining schema backlog now starts at Path 12 instead of Path 11.
- Migrated all seven Path 10 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full Cowork admin-operations and team-rituals path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 10 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `68` non-blocking warnings, so the remaining schema backlog now starts at Path 11 instead of Path 10.
- Migrated all seven Path 09 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full Cowork documents, research, and data-extraction path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 09 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `75` non-blocking warnings, so the remaining schema backlog now starts at Path 10 instead of Path 09.
- Migrated all seven Path 08 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full Claude Cowork path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 08 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `82` non-blocking warnings, so the remaining schema backlog now starts at Path 09 instead of Path 08.
- Migrated all six Path 07 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full cross-surface Claude path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 07 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `89` non-blocking warnings, so the remaining schema backlog now starts at Path 08 instead of Path 07.
- Migrated all seven Path 06 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full Git and GitHub path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 06 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `95` non-blocking warnings, so the remaining schema backlog now starts at Path 07 instead of Path 06.
- Migrated all eight Path 05 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full debugging, testing, and refactoring path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 05 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `102` non-blocking warnings, so the remaining schema backlog now starts at Path 06 instead of Path 05.
- Migrated all eight Path 04 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, so the full repo-navigation path now uses authored guided scaffolding instead of inferred fallback support.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 04 rollout and confirmed the migration moved cleanly to `139` lessons across `19` paths with `0` errors and `110` non-blocking warnings, so the remaining schema backlog now starts at Path 05 instead of Path 04.
- Migrated all seven Path 02 lessons and all seven Path 03 lessons onto `stepSchemaVersion: 1` with explicit `StepMeta` blocks after every walkthrough step heading, covering the earliest terminal/filesystem and Claude Code learner journeys with authored purpose/action/result/why/next/tutor prompts instead of inferred fallback scaffolding.
- Re-ran `python tooling/scripts/validate_content.py` after the Path 02 and Path 03 migration and confirmed the rollout moved cleanly to `139` lessons across `19` paths with `0` errors and `118` non-blocking warnings, so the remaining schema backlog now starts at Path 04 instead of Path 02.
- Made Learning Mode affect lesson scaffolding in the actual React render path instead of only through tutor behavior and CSS hiding: guided shows the full scaffold, hint mode reduces to goal plus expected result, and independent mode collapses to checkpoint-oriented support.
- Added fallback step support for legacy walkthrough lessons that do not yet declare `stepSchemaVersion: 1`, so every rendered step can still expose a local tutor handoff, inferred action/checkpoint copy, and next-step guidance while schema migration is still underway.
- Updated `tooling/scripts/validate_content.py` and `content/README.md` to treat missing step schema as a visible migration signal: the validator now warns by default, supports `--strict-step-schema`, and initially surfaced the remaining non-blocking migration backlog across the later paths with `0` content errors.
- Revalidated the new guided-step runtime live in the browser on both a schema-backed Path 01 lesson and a legacy Path 03 lesson, confirming that mode changes visibly alter scaffolding and that step-local tutor requests open the floating tutor with the exact current step already preloaded.
- Added an authenticated Playwright regression for the lesson-page tutor shell in `apps/web/e2e/tutor.spec.ts`. The spec logs in through `/login?next=...`, verifies the chat-first default state, checks that helper chrome stays behind the Tools drawer, and cleanly skips when `E2E_USER_EMAIL` / `E2E_USER_PASSWORD` are not configured.
- Simplified the floating tutor on real lesson pages into a chat-first surface: removed the always-visible mode bar, first-run capability panels, and inline prompt chip clutter from the main panel; moved mode switching, prompt shortcuts, grounding, and quick links behind the Tools drawer; and restored the full-width conversation area for actual back-and-forth.
- Revalidated that tutor usability pass with clean frontend `npm run lint`, clean `npm run typecheck`, clean production `npm run build`, and live browser QA on `/learn/01-start-here/lesson-1-2-1-make-a-personal-safety-checklist` confirming the lesson-page tutor now opens into a mostly full-width chat surface with helper controls available on demand.
- Reworked the floating tutor into a much wider, more usable panel with a richer first-run briefing, larger conversation area, inline quick prompts, and a dedicated tools rail on large screens so learners can actually read and use long tutor answers.
- Fixed the false "Anthropic API key is missing" tutor fallback by teaching the Next tutor runtime to resolve `ANTHROPIC_API_KEY` from the repo root `.env` as a server-side fallback when the app-local env file is empty in local monorepo development.
- Added the first schema-backed guided-step system to the learner engine: `LessonContent.tsx` now recognizes per-step `<StepMeta ... />` blocks, walkthrough steps render through a reusable step card, and the lesson shell can vary the visible scaffold by learning mode instead of only changing tutor behavior.
- Extended the shared tutor runtime so lesson steps can launch a local "I'm stuck here" flow with step-specific context, letting learners open the floating tutor from the exact step where they are blocked without restating the whole lesson.
- Updated the content validator and content authoring contract so schema-enabled lessons can declare `stepSchemaVersion: 1` and then receive enforcement for one `StepMeta` block per walkthrough step.
- Migrated the full Path 01 foundation path onto the new step schema so the beginner entry flow now has purpose, action, expected result, why-it-matters, next-step setup, and step-specific tutor prompts across every foundational lesson.
- Reworked the live lesson reading experience around the guided-learning engine instead of raw markdown flow: the web app now groups lesson content into explicit modules, elevates Objective / Walkthrough / Verification as first-class sections, turns walkthrough steps into scan-friendly cards, and tracks sidebar progress against rendered lesson modules instead of brittle direct-child heading assumptions.
- Standardized frontend typography for lesson-heavy surfaces around Inter and JetBrains Mono, raised desktop reading size to an 18px body rhythm, increased mono/code readability, and shifted hierarchy toward weight and spacing rather than oversized heading jumps.
- Tightened lesson rendering semantics in `LessonContent.tsx` by stripping duplicate body H1s, recognizing markdown blockquotes as visual notes, styling inline code and tables consistently, and giving verification its own dedicated section so the end state of each lesson is clearer.
- Cleaned up a few content-level rough edges in the live curriculum by modernizing the recovery command in the safety checklist lesson and converting the repo-cloning risk note in Path 04 into a structured warning block for consistent learner presentation.
- Implemented authenticated local-to-hosted backfill for legacy browser-only lesson completions: authenticated app surfaces now POST old local completions into `/api/progress`, preserve original completion timestamps, remove successfully synced local keys, and refresh the signed-in dashboard state after successful backfill.
- Tightened browser-local lesson progress so it is now user-scoped instead of browser-global, which prevents one learner's local fallback progress from being surfaced or backfilled into another learner's account on a shared machine. Legacy anonymous local progress is still readable and backfillable when no competing user scopes exist.
- Fixed the remaining console-noise issues in the learner flow: `app/layout.tsx` now suppresses root hydration warnings caused by external `<html>` attribute injection, `LessonContent.tsx` now preserves fenced code blocks when splitting lesson bodies, and `LessonPlayer.tsx` now reads only direct lesson-body headings with stable section keys.
- Revalidated the learner-flow changes end to end in the authenticated browser session: the lesson page reload is now clean, dashboard hydration remains correct, a seeded legacy local completion for `lesson-1-2-1-make-a-personal-safety-checklist` backfilled into hosted progress successfully, and the dashboard completed count advanced from `2` to `3` with the local key removed afterward.
- Reworked the tutor runtime so the floating tutor now uses a shared runtime provider, mode-aware gating, richer signed-in state, and `/api/tutor` capability/usage responses that distinguish free and Pro tutor modes.
- Final validation on the current tree is green: frontend `npm run typecheck` passes, production `npm run build` passes, and the final review pass found no remaining blocking issues in the progress-sync diff.
- Verified the hosted cross-device lesson-sync repair end to end in the signed-in browser session for `findbene+cliacademy-d01a1a04@gmail.com`: authenticated `GET /api/progress` for lesson `1.1.2` returned `200` with `completed: false`, authenticated `POST /api/progress` returned `200` with `Progress saved.`, and a second `GET` returned a real Supabase-backed `lesson_id`, `completed_at`, and stored `completion_data`.
- Fixed the last blocker inside the new hosted curriculum sync: live MDX frontmatter stores dotted lesson labels like `1.1.1`, so `apps/web/lib/mdx.ts` now preserves those labels for learner-facing UI while deriving stable integer sort order values for sorting and Supabase `lessons.sort_order` writes.
- Reconciled the dashboard hydration path so completed-lesson totals now merge hosted progress keys with browser-local fallback keys without double counting. In the verified browser session the dashboard now renders `2` completed lessons and advances the continue-learning card to `Make a personal safety checklist` with `2 of 7 lessons completed`.
- Confirmed the local environment is no longer the blocker: `apps/web/.env` now carries the required service-role key, the hosted self-heal runs locally, and the repaired Supabase project schema plus explicit `public` schema client configuration are working together.
- Repaired the stale hosted lesson-progress mapping in code by adding a server-side curriculum sync that upserts the live `PATHS` plus MDX lesson catalog into Supabase `paths`, `modules`, and `lessons`, then retries `/api/progress` lesson resolution against the refreshed hosted rows.
- Wired the same hosted-catalog self-heal into the signed-in dashboard and learner path page so server-rendered completion state can resolve the current live curriculum instead of the stale seeded catalog.
- Revalidated the hosted-sync repair with clean frontend `npm run lint`, clean `npm run typecheck`, clean production `npm run build`, and green Playwright smoke coverage (`18/18`) in `apps/web`.
- Completed the final authenticated browser revalidation with the newly confirmed QA alias `findbene+cliacademy-d01a1a04@gmail.com`: sign-in succeeded, onboarding completed cleanly, `Mark complete` on the first Start Here lesson switched to the local-fallback success state, and the dashboard updated to `1` completed lesson while advancing the continue-learning card to the next lesson.
- Confirmed the user-facing fix is effective even though hosted sync still misses the published lesson mapping in the background: the lesson page reports `Progress saved on this device while lesson sync catches up.`, the lesson is marked complete, and the dashboard reflects completion immediately in the same browser.
- Implemented a learner-facing resilience fix for the published-path mismatch in hosted progress sync: lesson completion now persists locally in the browser, and the dashboard hydrates its completed-count plus continue-learning card from merged server and local progress data.
- Revalidated that lesson-progress fallback change with clean frontend `npm run lint`, clean `npm run typecheck`, green Playwright smoke coverage, and a clean production `npm run build` in `apps/web`.
- Completed the authenticated browser pass with a confirmed Supabase-backed user: login succeeded, `/onboarding` redirected completed users back to `/dashboard`, the dashboard resume card opened the expected lesson, lesson verification required learner evidence and returned real feedback, and sign-out returned cleanly to `/login`.
- Found a real learner-facing regression in authenticated progress persistence: clicking `Mark complete` on `/learn/01-start-here/lesson-1-1-1-create-the-cli-academy-workspace` surfaces `The requested lesson could not be matched to a published path.`, emits a 404-backed failure in the sidebar flow, and leaves dashboard progress unchanged at `0 of 7 lessons completed`.
- Attempted the full authenticated browser pass with the supplied QA credentials and confirmed the active auth environment still requires confirmed email accounts: signing in with `test.learner+qa@yourdomain.com` failed, and signing up the same address created a brand-new unconfirmed user.
- Confirmed the local web app is pointed at a hosted Supabase project and `apps/web/.env` has no service-role key, so the repo cannot self-confirm disposable QA users without inbox access or an externally confirmed seeded account.
- Refreshed the two highest-traffic public hero surfaces: `/` now uses a Spline-powered 3D hero card via `apps/web/components/ui/splite.tsx`, while `/lounge` reuses the former front-page shader animation inside `HeroPick` after making `ShaderBackground` container-aware.
- Revalidated that marketing-surface refresh with clean `npm run lint` and clean production `npm run build` in `apps/web`.
- Re-ran a focused editorial QA pass on the rewritten live Paths 10-16 and confirmed the live lesson corpus already satisfies the structural learner contract, so no curriculum edits were needed from that pass.
- Resumed browser verification on the real local app, confirmed unauthenticated protected-route redirects and callback `next` preservation, and isolated the remaining learner-flow gap to lack of a sanctioned signed-in test account rather than a route/runtime failure.
- Fixed a real auth regression by replacing the dead `Forgot password?` CTA on `/login` with a working recovery flow: added `/forgot-password` and `/reset-password` routes, request/update password cards, and smoke coverage for the new pages plus the login CTA target.
- Revalidated the frontend after the auth recovery fix with clean `npm run lint`, clean `npm run typecheck`, clean `npm run build`, and green Playwright smoke coverage (`18/18`).
- Normalized the live curriculum structure back to the canonical `content/paths/` slugs for Paths 01-19 by archiving stale duplicate trees/files and moving the rewritten Path 11, 12, 14, 15, and 16 content into the stable live directories.
- Hardened `apps/web/lib/mdx.ts` so live lesson loading only includes lesson-pattern MDX files, preventing orphan or duplicate content files from leaking into learner routes.
- Rebuilt `tooling/scripts/validate_content.py` to match the live learner contract: recursive chapter discovery, `lesson-*.mdx` filtering, current frontmatter fields (`title`, `lessonNumber`, `chapterNumber`, `pathNumber`), and ignored-file warnings for non-lesson MDX.
- Updated `content/README.md` so the documented lesson path shape and frontmatter baseline match the live chapterized curriculum instead of the older flat structure.
- Fixed malformed frontmatter in seven Path 17 lesson files and archived the obsolete `nano_claw_organizer.mdx` lab out of the live Path 13 tree into `content/paths_old/2026-04-curriculum-rewrite/...`.
- Revalidated the curriculum after those repairs: the validator now reports 139 live lessons across 19 paths with 0 errors and 0 warnings.
- Added `apps/web/lib/learning.ts` and rewired onboarding completion plus dashboard recommendations/resume behavior to the current live path slugs and first-lesson routes.
- Added mobile app navigation in `AppSidebar`, lesson section progress tracking in `LessonPlayer`, and a real API-backed verification flow through `apps/web/app/api/lesson-verification/route.ts`.
- Updated path metadata for migrated curriculum and added the DOM marker needed for lesson-progress tracking without changing route contracts.
- Revalidated the frontend after these changes with clean `npm run lint`, clean `npm run typecheck`, clean `npm run build`, and green Playwright smoke coverage (`16/16`).
- Rewrote all seven Path 18 MDX lessons under `content/paths/18-real-world-agent-builds-for-everyday-productivity/` to remove template-only `touch`/`nano` loops and replace them with real Claude Code + `CLAUDE.md` assistant workflows.
- Added dual command paths (macOS/Linux and Windows PowerShell) in every lesson walkthrough.
- Added "What can go wrong" troubleshooting tables in every lesson with real risks: runaway loops, over-permission, and avoidable cost overruns.
- Realigned each `VerificationBlock` so deliverables and checks match the actual hands-on activities performed by learners.
- Replaced the older Path 04-09 chapter trees with new practical lesson packs covering codebase mapping, controlled repo operations, debugging/testing/refactoring, Git/GitHub workflows, cross-surface Claude usage, Cowork session discipline, and research/document workflows.
- The working tree also includes the same replacement pattern for later paths that had stale chapter structures (notably Paths 10-16), with old directories removed and new chapter directories staged for the live curriculum surface.
- Rewrote all nine Path 19 MDX lessons under `content/paths/19-capstones-portfolio-proof-and-job-ready-evidence/` around portfolio-grade capstones instead of file-creation drills.
- Standardized Capstone 1 as a bounded daily git digest, Capstone 2 as a real TODO extraction workflow, and Capstone 3 as an integrated git-plus-content curriculum brief pilot.
- Added dual command paths, real troubleshooting tables, and verification checks that match the actual capstone artifacts: scope docs, run logs, reports, case studies, and final portfolio pack.
